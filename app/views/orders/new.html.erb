<%
    provide(:header, "New Order")
    provide(:title, "New Order")
    provide(:button_text, "Save Order")
%>
<h4><strong>Order Season:</strong> <%=current_season.order_season_name%></h4>
<%= render('form')%>
