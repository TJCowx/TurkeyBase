<%
    provide(:header, 'Add a New Product')
    provide(:title, 'New Product')
    provide(:form_type, new_product_path)
    provide(:button_text, 'Add Product')
%>

<%= render 'form', {url: new_product_path} %>
