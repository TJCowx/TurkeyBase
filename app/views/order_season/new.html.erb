<% provide(:header, "Create a New Order Season")
   provide(:title, "Create Order Season")
   provide(:button_text, "Add Season")
   provide(:model, @order_season)%>

<%= render partial: "form", :object => @order_season %>
